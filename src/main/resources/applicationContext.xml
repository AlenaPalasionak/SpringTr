<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">
<context:property-placeholder location="classpath:musicPlayer.properties"/>
    <bean id="musicBean"
          class="org.example.RockMusic">
    </bean>

    <bean id="musicPlayer"
          class="org.example.MusicPlayer"
    scope = "prototype"> <!--чтобы каждый раз создавался новый объект-->
        <property name="music" ref="musicBean"/>    <!--внедр.завис. с помощью сеттера.
                                                    имя сеттера "music" без слова set и камэлКейса -->
        <!--<property name="name" value="Some name"/>
        <property name="volume" value="50"/>можно так внедрять значения переменных, второй вариант ниже-->
        <property name="name" value="${musicPlayer.name}"/>
        <property name="volume" value="${musicPlayer.volume}"/>

    </bean>

</beans>